package org.tud.inf.st.mbt.ulang.guigraph;

import org.eclipse.swt.graphics.Color;
import org.eclipse.swt.graphics.RGB;
import org.eclipse.swt.widgets.Display;

public class GGColors {
	public static final Color c0 = new Color(Display.getCurrent(), new RGB(255,
			255, 180));
	public static final Color c1 = new Color(Display.getCurrent(), new RGB(255,
			200, 40));
	public static final Color c2 = new Color(Display.getCurrent(), new RGB(200,
			210, 200));
	public static final Color c3 = new Color(Display.getCurrent(), new RGB(
			91, 87,159));
}
